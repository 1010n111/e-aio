# 文档地图与开发队列

## 设计文档（`docs/`，按「序号-项目名」命名）

| 文档 | 内容 | 状态 |
|---|---|---|
| `01-…-可行性研究报告.md` | 市场/技术/经济/组织/法律/进度/风险（GB/T 8567） | 已定稿 |
| `02-…-软件需求规格说明书.md` | SRS：22 组功能需求（FR-SEC/AUD/WF/APR/MDM/OA/CRM/…）+ 39 条非功能 | 已定稿 |
| `03-…-概要设计说明书.md` | HLD：22 模块、模块边界、API 规约（2.6.4）、开发队列（13.2，P0–P3 共 24 项） | 已定稿 |
| `04-…-详细设计说明书.md` | DD 总册索引（分册体系 + 统一约定 + 验收对照） | 已定稿 |
| `04-…-详细设计说明书-P0-工程地基.md` | 工程骨架（顺序 1）+ common 底座（顺序 2），含 3.10 RuoYi 改造 12 步 | 已定稿（tag `v0.1.0-p0`） |
| `04-…-详细设计说明书-P1-1-批次总册.md` | **当前批次入口**：P1（M1–M5）范围与里程碑、跨册契约裁决（依赖环/事件/前端/命名与 DDL/安全底线）、质量门增量 A1–A7、验收分段、模块/Schema/错误码段登记表、多册权威口径 | 编写完成（待评审） |
| `04-…-详细设计说明书-P1-2-平台底座.md` | P1 合并册：platform（顺序 3，M1–M3）+ iam（顺序 4，M2–M5），7 章齐全 | 编写完成（待评审） |
| `04-…-详细设计说明书-P1-3-platform.md` | platform 单模块册：按能力域展开的详细设计 + 20 表 DDL + 9 个 `api` 接口（**platform 契约与错误码号的权威来源**） | 编写完成（待评审） |
| `04-…-详细设计说明书-P1-4-iam.md` | iam 单模块册：组织/认证/授权/数据权限/SoD，31 表 + 10 接口 + 66 错误码（**iam 契约与错误码号的权威来源**） | 编写完成（待评审） |
| `04-…-详细设计说明书-P1-5-audit.md` | audit 分册（顺序 5，M4–M6）：双审计、WORM、哈希链、归档与告警，9 表 19 错误码 | 编写完成（待评审） |
| `04-…-详细设计说明书-P1-workflow-*.md` | 顺序 6–9（workflow+approval / mdm / oa / portal） | 待编写（M5 起） |

**分册子序号**：`-P<批次>-<序号>-<主题>.md`。P1 已分 5 册：`-1-批次总册`（入口）、`-2-平台底座`（platform+iam 合并册）、`-3-platform`、`-4-iam`、`-5-audit`；后续 workflow/approval（顺序 6）起继续用 `-6-`、`-7-`……

**ADR（`docs/adr/`）**：`0001` 统一 POST 与恒 200、`0002` 每模块独立 Schema 与 Flyway、`0003` 协议端点例外（OAuth2/SAML2/文件流，P1）、`0004` 跨 Schema 只读谓词例外（数据权限子树，P1）、`0005` platform 零反向依赖（自有 `OrgContextPort` + 应用壳装配，P1）。规约类文件以 ADR 为准，不得在分册里就地放宽。

`docs/agents/` 是代理指令目录（本套 AGENTS 分册），是 `docs/` 下唯一不遵循序号命名的目录。

该目录里另有一类**批次交接文档** `handoff-<批次>-<模块>.md`（给接手的代理或人看：当前进度与提交线、LIVE TODO、环境踩坑与复现命令、子代理派工模板、不能碰的冻结件）。它**不是评审基准**，与 AGENTS 分册的效力不同；现有：`handoff-P1-3-platform.md`。

另有两处非设计文档载体：根 [`CONTEXT.md`](../CONTEXT.md)（**术语表**：项目语言与契约名词的唯一定义处）与 [`docs/adr/`](../adr/)（**ADR**：难逆 + 需背景 + 真实取舍的决策，命名 `NNNN-<slug>.md`）。术语冲突以 CONTEXT.md 为准，已落 ADR 的决策以 ADR 为准（上游优先级仍见 [AGENTS.md](../../AGENTS.md)）。

## 开发队列（HLD 13.2）

P0 工程骨架 + common → P1 平台底座（platform/iam/audit/workflow/approval/mdm/…）→ P2 业务域（oa/crm/inventory/finance/hr/project/scm/…）→ P3 开放定制与 AI 增强。

写代码前先确认目标模块属于哪一批，不跨批次提前实现。常用章节：HLD 2.2 模块边界 / 2.6 API / 5.1 common / 13.2 队列。

## 新增文档

工程分册按「序号-项目名」命名；数据库设计并入对应详细设计分册的「数据结构设计」章节，不单独成文档。
